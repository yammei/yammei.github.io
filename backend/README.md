```bash
sudo npm init -y
sudo npm install typescript ts-node @types/node --save-dev
sudo npm i --save-dev @types/node
sudo npm install typescript --save-dev
sudo npx tsc --init

sudo chmod -R 777 .

sudo npm install
sudo npm install express
sudo npm install socket.io
sudo npm install redis
sudo npm install cors

sudo npm run build
sudo node server.js
```